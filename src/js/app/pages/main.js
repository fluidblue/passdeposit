/**
 * PassDeposit
 * 
 * @author Max Geissler
 */

define(
[
	"jquery",
	"domReady",
	"bootstrap"
],
function($, domReady)
{
	// Initialize page when DOM is ready.
	domReady(function()
	{
		// TODO
	});
});
